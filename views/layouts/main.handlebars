<!DOCTYPE html>
<html lang="et">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/styles.css">
    {{#if isDashboard}}
    <link rel="stylesheet" href="/css/dashboard.css">
    {{/if}}
</head>

<body>
    {{#unless isDashboard}}
    <header>
        <h1>{{heading}}</h1>
    </header>

    <main>
        {{{body}}}
    </main>

    <footer>
        <p>&copy; 2025 Uptime Tracker</p>
    </footer>

    <script src="/js/form-validation.js"></script>
    {{else}}
    {{{body}}}
    {{/unless}}
</body>

</html>